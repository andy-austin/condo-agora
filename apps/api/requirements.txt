# Runtime dependencies only (dev deps in pyproject.toml)
fastapi>=0.104.0
strawberry-graphql[fastapi]>=0.213.0
python-dotenv>=1.0.0
pydantic>=2.0.0

# Database (MongoDB)
motor>=3.3.0

# Auth (Clerk integration)
svix>=1.0.0
PyJWT[crypto]>=2.8.0
